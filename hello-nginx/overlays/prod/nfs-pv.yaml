apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-pv-prod
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: 10.0.0.200
    path: /srv/nfs/share
